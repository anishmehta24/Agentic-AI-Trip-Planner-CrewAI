# Windows-local install — same as requirements.txt minus pysqlite3-binary,
# which is a Linux-only (Streamlit Cloud) ChromaDB workaround with no Windows
# wheels. No project code imports it. Use requirements.txt for cloud deploy.

# Core Framework
streamlit>=1.33.0

# AI & LLM
crewai>=0.28.0
crewai[tools]>=0.28.0
langchain>=0.1.0
langchain-community>=0.0.10
langchain-core>=0.1.10
langchain-groq
langchain-google-genai

# LLM Providers & Utilities
litellm>=1.0.0
groq>=0.4.0
google-generativeai>=0.3.0

# Search Tools
duckduckgo-search>=4.0.0

# Utilities
python-dotenv>=1.0.0
watchdog
